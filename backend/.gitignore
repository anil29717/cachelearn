# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Environment variables
.env
.env.local
.env.*
!.env.example

# Dependencies
node_modules/
vendor/

# Build outputs
dist/
build/
# Uploaded library files and local data (never commit)
storage/
storage/seed-users.json

# Legacy / local demo seed copies (use scripts/seed-demo.mjs from repo)
scripts/seed-demo-hardcoded.mjs
scripts/*-secrets*.mjs
scripts/*-local.mjs
coverage/

# Local dev TLS — create with: npm run generate-cert (never commit private keys)
certs/*.pem

# IDE / Editor settings
.vscode/
.idea/
*.swp
