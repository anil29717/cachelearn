/**
 * Central Express API path prefixes (keep in sync with the frontend `api` client).
 */
export const API_MOUNTS = {
  AUTH: '/api/auth',
  ADMIN: '/api/admin',
  USERS: '/api/users',
  LIBRARY: '/api/library',
};
